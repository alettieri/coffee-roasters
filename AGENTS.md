## Agent skills

### Issue tracker

Issues are tracked in GitHub Issues; external PRs are not a triage surface. See `docs/agents/issue-tracker.md`.

### Triage labels

Uses the five default triage labels. See `docs/agents/triage-labels.md`.

### Domain docs

This is a single-context repository. See `docs/agents/domain.md`.

### Repository operating contract

Before changing code or infrastructure, read `docs/agents/operating-contract.md`.

### Architectural decisions

Use the active decision index at `docs/adr/README.md`. Superseded ADRs are historical context, not current instructions.
